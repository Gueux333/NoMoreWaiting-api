<h1>Update Place</h1>

<form action="/places/update/{{place.id}}" method="post">
  <div class="form-group">
    <label for="name">Name*</label>
    <input value="{{place.name}}" class="form-control" type="text" id="name" name="name" placeholder="Nom du concert" required>
  </div>

  <div class="form-group">
    <label for="venue">Venue*</label>
    <input value="{{place.venue}}" class="form-control" type="text" id="venue" name="venue" placeholder="Nom de la salle" required>
  </div>

  <div class="form-group">
    <label for="description">Description</label>
    <input value="{{place.description}}" class="form-control" type="text" id="description" name="description" placeholder="Description du concert">
  </div>

  <div class="form-group">
    <label for="capacity">Capacity</label>
    <input value="{{place.capacity}}" class="form-control" type="number" id="capacity" name="capacity" placeholder="Ex: 500">
  </div>

  <div class="form-group">
    <label for="price">Price*</label>
    <input value="{{place.price}}" class="form-control" type="number" id="price" name="price" placeholder="Ex: 30" required>
  </div>

  <div class="form-group">
    <label for="image">Image</label>
    <input value="{{place.image}}" class="form-control" type="text" id="image" name="image" placeholder="Ex: http://lien.vers-l-image.com/img.jpeg">
  </div>

  <div class="form-group">
    <label for="date">Date</label>
    <input value="{{place.date}}" class="form-control" type="date" id="date" name="date">
  </div>

  <div class="form-group">
    <label for="lat">Latitude</label>
    <input value="{{place.lat}}" class="form-control" type="text" id="lat" name="lat" placeholder="Latitude">
  </div>

  <div class="form-group">
    <label for="lng">Longitude</label>
    <input value="{{place.lng}}" class="form-control" type="text" id="lng" name="lng" placeholder="Longitude">
  </div>

  <div class="text-center">
    <button type="submit" class="btn btn-primary">
      Update
    </button>
  </div>
</form>